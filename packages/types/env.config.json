{
  "$schema": "../env-tools/schemas/env-config-schema.json",
  "appName": "AI.C9d",
  "displayName": "C9D TypeScript Types",
  "description": "Shared TypeScript type definitions for the C9D AI platform",
  "envVars": {
    "required": [],
    "optional": [
      {
        "name": "NODE_ENV",
        "description": "Node.js environment",
        "type": "string",
        "enum": ["development", "production", "test"],
        "default": "development"
      }
    ]
  },
  "validation": {
    "strict": false,
    "warnOnMissing": false,
    "failOnInvalid": false
  }
}